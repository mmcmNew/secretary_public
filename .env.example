# Copy this file to .env and fill real secrets.
# Used by server/app/config.py and local runs.

# App runtime
MODE=development
HOST=0.0.0.0
SERVER_PORT=5100

# Security
SECRET_KEY=change_me_replace_with_a_strong_random_value
# Optional: if empty, JWT_SECRET_KEY will fall back to SECRET_KEY
JWT_SECRET_KEY=

# AI webhooks (keep placeholders or replace)
AI_WEBHOOK_URL=https://n8n.ndomen.online/webhook/4999da23-b30e-494b-9435-9948be5ab8d4
AI_IMAGE_WEBHOOK_URL=https://n8n.ndomen.online/webhook/2da3e25d-1ff6-41f9-94e8-d55d43c1247b

# Session settings
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
PERMANENT_SESSION_LIFETIME=3600

# Database (defaults for local dev).
# In Docker environments ensure host matches your postgres service (commonly 'db' or 'postgres').
POSTGRES_URI=postgresql+psycopg2://secretary:secretary@localhost:5432/secretary_db
DATABASE_URL=${POSTGRES_URI}

# Test DB
POSTGRES_TEST_URI=postgresql+psycopg2://secretary:secretary@localhost:5432/secretary_test_db
