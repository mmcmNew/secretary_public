# Copy this file to .env.docker and update secrets before running docker-compose --env-file .env.docker up

# Production-like defaults
MODE=production
NODE_ENV=production
HOST=0.0.0.0
SERVER_PORT=5100

# Secrets - replace with strong values or set via Docker secrets
SECRET_KEY=change_me_prod_secret
JWT_SECRET_KEY=

# Webhooks
AI_WEBHOOK_URL=https://n8n.ndomen.online/webhook/4999da23-b30e-494b-9435-9948be5ab8d4
AI_IMAGE_WEBHOOK_URL=https://n8n.ndomen.online/webhook/2da3e25d-1ff6-41f9-94e8-d55d43c1247b

SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
PERMANENT_SESSION_LIFETIME=3600

# Database for docker-compose: use the service name for host (example: 'db' or 'postgres')
DATABASE_URL=postgresql+psycopg2://secretary:secretary@db:5432/secretary_db

# Note: DO NOT put Docker Hub credentials or other secrets into these example files. Use GitHub repo secrets for CI.
